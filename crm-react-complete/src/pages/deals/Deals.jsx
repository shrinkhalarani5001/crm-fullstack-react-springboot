import{useMemo,useState}from"react";import{Link}from"react-router-dom";import SearchBar from"../../components/common/SearchBar";import DealTable from"../../components/deals/DealTable";import{dealService}from"../../services/dealService";import{useCRM}from"../../context/CRMContext";
export default function Deals(){const{deals,refresh}=useCRM();const[q,setQ]=useState("");const a=useMemo(()=>deals.filter(x=>JSON.stringify(x).toLowerCase().includes(q.toLowerCase())),[deals,q]);async function d(id){if(confirm("Delete this deal?")){await dealService.remove(id);refresh()}}return <div><div className="page-title"><div><h1>Deals</h1><p>Manage opportunities and revenue pipeline.</p></div><Link className="btn btn-primary" to="/deals/add">+ Add Deal</Link></div><div className="card"><SearchBar value={q} onChange={setQ} placeholder="Search deals..."/><DealTable items={a} onDelete={d}/></div></div>}
