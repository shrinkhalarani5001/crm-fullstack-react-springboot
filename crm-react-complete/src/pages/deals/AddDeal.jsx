import{useNavigate,Link}from"react-router-dom";import DealForm from"../../components/deals/DealForm";import{dealService}from"../../services/dealService";import{useCRM}from"../../context/CRMContext";
export default function AddDeal(){const nav=useNavigate();const{customers,refresh}=useCRM();return <div><div className="page-title"><div><h1>Add Deal</h1><p>Create a new sales opportunity.</p></div><Link className="btn btn-light" to="/deals">Back</Link></div><div className="card"><DealForm customers={customers} onSubmit={async f=>{await dealService.create(f);await refresh();nav("/deals")}}/></div></div>}
