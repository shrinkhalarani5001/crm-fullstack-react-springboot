import { useEffect,useState } from "react"; import {Link,useNavigate,useParams} from "react-router-dom"; import CustomerForm from "../../components/customers/CustomerForm"; import {customerService} from "../../services/customerService"; import {useCRM} from "../../context/CRMContext";
export default function CustomerDetails(){const {id}=useParams();const nav=useNavigate();const {refresh}=useCRM();const [c,setC]=useState(null);useEffect(()=>{customerService.getById(id).then(setC)},[id]);if(!c)return <div className="card">Loading...</div>;return <div><div className="page-title"><div><h1>Customer Details</h1><p>Edit customer information.</p></div><Link className="btn btn-light" to="/customers">Back</Link></div><div className="card"><CustomerForm initial={c} submitLabel="Update Customer" onSubmit={async f=>{await customerService.update(id,f);await refresh();nav("/customers")}}/></div></div>}
