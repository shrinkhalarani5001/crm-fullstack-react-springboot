import { useMemo,useState } from "react"; import { Link } from "react-router-dom"; import SearchBar from "../../components/common/SearchBar"; import CustomerTable from "../../components/customers/CustomerTable"; import {customerService} from "../../services/customerService"; import {useCRM} from "../../context/CRMContext";
export default function Customers(){const {customers,refresh}=useCRM();const [q,setQ]=useState("");const filtered=useMemo(()=>customers.filter(x=>JSON.stringify(x).toLowerCase().includes(q.toLowerCase())),[customers,q]);async function del(id){if(confirm("Delete this customer?")){await customerService.remove(id);refresh()}}return <div><div className="page-title"><div><h1>Customers</h1><p>Manage customer accounts and contact information.</p></div><Link className="btn btn-primary" to="/customers/add">+ Add Customer</Link></div><div className="card"><SearchBar value={q} onChange={setQ} placeholder="Search customers..."/><CustomerTable items={filtered} onDelete={del}/></div></div>}
