import { Link,useNavigate } from "react-router-dom"; import CustomerForm from "../../components/customers/CustomerForm"; import {customerService} from "../../services/customerService"; import {useCRM} from "../../context/CRMContext";
export default function AddCustomer(){const nav=useNavigate();const {refresh}=useCRM();return <div><div className="page-title"><div><h1>Add Customer</h1><p>Create a new customer record.</p></div><Link className="btn btn-light" to="/customers">Back</Link></div><div className="card"><CustomerForm onSubmit={async f=>{await customerService.create(f);await refresh();nav("/customers")}}/></div></div>}
