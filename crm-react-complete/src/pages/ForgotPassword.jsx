import { useState } from "react";
import { Link } from "react-router-dom";
import { useAuth } from "../hooks/useAuth";
export default function ForgotPassword(){const {resetPassword}=useAuth();const [f,setF]=useState({email:"",newPassword:""});const [msg,setMsg]=useState("");const [err,setErr]=useState("");async function s(e){e.preventDefault();setMsg("");setErr("");try{const r=await resetPassword(f.email,f.newPassword);setMsg(r.message)}catch(x){setErr(x.response?.data?.message||"Reset failed")}}return <div className="auth-page"><div className="auth-card"><div className="auth-brand">C</div><h1>Reset password</h1><p className="muted">Enter your account email and a new password.</p>{msg&&<div className="success">{msg}</div>}{err&&<div className="alert">{err}</div>}<form onSubmit={s}><label>Email<input type="email" required value={f.email} onChange={e=>setF({...f,email:e.target.value})}/></label><label>New Password<input type="password" minLength="6" required value={f.newPassword} onChange={e=>setF({...f,newPassword:e.target.value})}/></label><button className="btn btn-primary wide">Reset Password</button></form><div className="auth-links"><Link to="/login">Back to Login</Link></div></div></div>}
