import {useMemo,useState} from "react";import {Link}from"react-router-dom";import SearchBar from "../../components/common/SearchBar";import LeadTable from "../../components/leads/LeadTable";import {leadService}from"../../services/leadService";import{useCRM}from"../../context/CRMContext";
export default function Leads(){const{leads,refresh}=useCRM();const[q,setQ]=useState("");const a=useMemo(()=>leads.filter(x=>JSON.stringify(x).toLowerCase().includes(q.toLowerCase())),[leads,q]);async function d(id){if(confirm("Delete this lead?")){await leadService.remove(id);refresh()}}return <div><div className="page-title"><div><h1>Leads</h1><p>Track prospects through your sales process.</p></div><Link className="btn btn-primary" to="/leads/add">+ Add Lead</Link></div><div className="card"><SearchBar value={q} onChange={setQ} placeholder="Search leads..."/><LeadTable items={a} onDelete={d}/></div></div>}
