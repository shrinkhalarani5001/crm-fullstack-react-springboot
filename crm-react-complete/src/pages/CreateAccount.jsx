import { useState } from "react";
import { Link,useNavigate } from "react-router-dom";
import { useAuth } from "../hooks/useAuth";
export default function CreateAccount(){const {register}=useAuth();const nav=useNavigate();const [f,setF]=useState({fullName:"",email:"",password:""});const [err,setErr]=useState("");const c=e=>setF({...f,[e.target.name]:e.target.value});async function s(e){e.preventDefault();setErr("");try{await register(f);nav("/dashboard",{replace:true})}catch(x){setErr(x.response?.data?.message||"Registration failed")}}return <div className="auth-page"><div className="auth-card"><div className="auth-brand">C</div><h1>Create account</h1><p className="muted">Set up your CRM account</p>{err&&<div className="alert">{err}</div>}<form onSubmit={s}><label>Full Name<input name="fullName" required value={f.fullName} onChange={c}/></label><label>Email<input name="email" type="email" required value={f.email} onChange={c}/></label><label>Password<input name="password" type="password" minLength="6" required value={f.password} onChange={c}/></label><button className="btn btn-primary wide">Create Account</button></form><div className="auth-links"><Link to="/login">Back to Login</Link></div></div></div>}
