import {useCRM} from "../context/CRMContext";
export default function Reports(){const{customers,leads,deals,tasks}=useCRM();const revenue=deals.filter(x=>x.status==="WON"||x.stage==="CLOSED_WON").reduce((s,x)=>s+(Number(x.amount)||0),0);return <div><div className="page-title"><div><h1>Reports</h1><p>Current CRM performance summary.</p></div></div><div className="stats-grid"><div className="stat-card"><div><div className="muted">Total Revenue</div><div className="stat-value">₹{revenue.toLocaleString()}</div></div></div><div className="stat-card"><div><div className="muted">Customers</div><div className="stat-value">{customers.length}</div></div></div><div className="stat-card"><div><div className="muted">Leads</div><div className="stat-value">{leads.length}</div></div></div><div className="stat-card"><div><div className="muted">Tasks</div><div className="stat-value">{tasks.length}</div></div></div></div><div className="card"><h3>Pipeline</h3><table><thead><tr><th>Stage</th><th>Deals</th><th>Amount</th></tr></thead><tbody>{["PROSPECTING","QUALIFICATION","PROPOSAL","NEGOTIATION","CLOSED_WON","CLOSED_LOST"].map(s=><tr key={s}><td>{s}</td><td>{deals.filter(d=>d.stage===s).length}</td><td>₹{deals.filter(d=>d.stage===s).reduce((a,d)=>a+(Number(d.amount)||0),0).toLocaleString()}</td></tr>)}</tbody></table></div></div>}
