import{useMemo,useState}from"react";import{Link}from"react-router-dom";import SearchBar from"../../components/common/SearchBar";import TaskTable from"../../components/tasks/TaskTable";import{taskService}from"../../services/taskService";import{useCRM}from"../../context/CRMContext";
export default function Tasks(){const{tasks,refresh}=useCRM();const[q,setQ]=useState("");const a=useMemo(()=>tasks.filter(x=>JSON.stringify(x).toLowerCase().includes(q.toLowerCase())),[tasks,q]);async function d(id){if(confirm("Delete this task?")){await taskService.remove(id);refresh()}}return <div><div className="page-title"><div><h1>Tasks</h1><p>Track activities and follow-ups.</p></div><Link className="btn btn-primary" to="/tasks/add">+ Add Task</Link></div><div className="card"><SearchBar value={q} onChange={setQ} placeholder="Search tasks..."/><TaskTable items={a} onDelete={d}/></div></div>}
