import{useNavigate,Link}from"react-router-dom";import TaskForm from"../../components/tasks/TaskForm";import{taskService}from"../../services/taskService";import{useCRM}from"../../context/CRMContext";
export default function AddTask(){const nav=useNavigate();const{customers,leads,refresh}=useCRM();return <div><div className="page-title"><div><h1>Add Task</h1><p>Create a follow-up activity.</p></div><Link className="btn btn-light" to="/tasks">Back</Link></div><div className="card"><TaskForm customers={customers} leads={leads} onSubmit={async f=>{await taskService.create(f);await refresh();nav("/tasks")}}/></div></div>}
