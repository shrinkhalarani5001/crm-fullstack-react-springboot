export default function RecentCustomers({ customers }) { return <div className="card"><div className="card-head"><h3>Recent Customers</h3></div>{customers.slice(0,5).map(c=><div className="list-row" key={c.id}><div className="avatar">{c.name?.charAt(0)}</div><div><b>{c.name}</b><small>{c.company || c.email}</small></div><span className="badge">{c.status}</span></div>)}{!customers.length&&<div className="empty">No customers yet.</div>}</div>; }
